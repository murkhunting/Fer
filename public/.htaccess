# Forzar HTTPS (descomentar en producción si el dominio ya tiene SSL activo)
# <IfModule mod_rewrite.c>
#   RewriteEngine On
#   RewriteCond %{HTTPS} off
#   RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]
# </IfModule>

# No listar directorios por defecto
Options -Indexes
