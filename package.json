{
  "name": "fer",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "php:api": "php -S localhost:8001 -t public",
    "build:hosting": "astro build && rm -f dist/api/config.php dist/api/config.example.php && echo 'dist listo sin credenciales'",
    "sync:down": "node scripts/sync.mjs down",
    "sync:up": "node scripts/sync.mjs up"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.3.2",
    "gsap": "^3.15.0",
    "tailwindcss": "^4.3.3"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "basic-ftp": "^6.2.1"
  }
}
